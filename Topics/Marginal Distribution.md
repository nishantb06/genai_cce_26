For two [[Random Variables]] X and Y and :

$$P_X(a) = \int P_{XY}(a, b)\, db \quad \text{(marginal)}$$
$$p_\theta(x) = \int p_\theta(x, z)\, dz \quad \text{(marginal over } z \text{)}$$
![[images/Pasted image 20260618194405.png]]
Sum along either of the two axes to get the marginal distribution

Related :
1. [[Joint Distribution]]
2. 