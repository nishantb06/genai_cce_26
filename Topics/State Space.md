We define the possible outcomes of a [[Random Variables]] to be its state space. 
That is the Range of the function known as a Random variable 

We define the set of possible values of the random variable to be its state space, denoted

X(Ω) = X. We define the probability of any given state using

pX(a) = P[X= a] = P[X−1(a)]

where X−1(a) = {ω∈Ω|X(ω) = a}is the pre-image of a
