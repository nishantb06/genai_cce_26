Given a true distribution $P_X$ with density $p_X(x)$ and a model $P_\theta$ with density $p_\theta(x)$:

$$\int_{\mathcal{X}} p_X(x)\, dx = 1 \quad \text{(valid probability measure)}$$

> Note: $p_\theta(x)$ is the *likelihood* of $x$ under $\theta$, not necessarily a probability measure by itself.


Likelihood is an element in the range space of a the [[Probability Density]] function p_theta(x). 

Integrating likelihood over all the valid random variables gives us 1! so can we say that likelihood is the slope of the function which is called probability measure ?

--- from Prob ML 
Likelihood is the conditional probability of the data given the hypotheses


