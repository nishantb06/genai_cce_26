We all know how to compute these from a dataset. For example, to compute the median of a dataset, we sort the data and pick the number that sits in the 50th percentile. However, the median computed in this way is the empirical median, i.e., it is a value computed from a particular dataset. If the data is generated from a random variable (with a given PDF), how do we compute the mean, median, and mode?

how do we compute the median if we are given a [[Random Variables | random variable]] $X$ with a PDF $f_X(x)$? The answer is by integrating the PDF.

[[Median of a Random Variable]]
[[Mode of a Random Variable]] 
The Mean is of course the [[Expectations | expectation]]

But we can introduce a method to [[Calculate the Expectation from the CDF]] 
