Number of tokens needed on average to represent one word. Fertility is calculated per tokenizer per language and used to compare two tokenizers. 

The best tokenizer of openai has fertility of English as 1.232

Look for this metric in every tokenizer paper

