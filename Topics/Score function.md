Gradient of the log of a particular distribution wrt the underlying Random variable is called the score function. 
$$\nabla_t \log p(t)$$

What is the direction in which the log likelihood is peaking 

Traverse in the direction that the score function points

